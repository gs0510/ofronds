(* Converting a string to an int *)
let a = 5 + int_of_string "10"

let x = 3
(* Convert an int to a string *)
let s = "There are " ^ x ^ " errors in this exercise."

(* Leave this line as is *)
let b = 4.0
(* Convert a float to an int *)
let c = 1 + b

let d = -3
(* Convert an int to a float *)
let e = 2.5 -. d
