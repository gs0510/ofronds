(* OCaml is strongly typed; types are often inferred by the compiler.
It's possible to declare types inline as done in this exercise,
but that's not a common practice. *)

let x : int = 5
let y : float = "hello"
